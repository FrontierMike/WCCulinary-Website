# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*

# environment variables
.env
.env.production
.dev.vars

# wrangler local state
.wrangler/

# The booking log holds real client names and deal values. Kept locally and
# backed up wherever Jan keeps her business records — never in the repo.
booking-log.xlsx

# macOS-specific files
.DS_Store

# The client's original photography (~300MB of 6000px files). Kept locally;
# `npm run images` derives the committed set in src/assets/images/.
# Deliberately NOT under public/ — public/ is copied into dist verbatim.
originals/
